# 塗装会社専用AI積算・見積りシステム
# Phase1 依存パッケージ

# Web UI
# 1.37.0 で st.fragment / st.rerun(scope="fragment") / st.dialog が安定版になった。
# クリック計測の部分再実行（fragment）と拡大ダイアログ（dialog）に必須のため 1.37 を下限とする。
streamlit>=1.37.0

# OpenAI API（LLM・画像解析・音声認識）
openai>=1.30.0

# 環境変数管理
python-dotenv>=1.0.0

# Excel出力
openpyxl>=3.1.2

# PDF出力
reportlab>=4.2.0

# データ処理
pandas>=2.0.0

# 画像処理（将来的な前処理用）
Pillow>=10.0.0

# ─────────────────────────────────────
# Phase2以降（必要になったら追加）
# ─────────────────────────────────────
# pytesseract       # OCR（図面テキスト抽出）
pymupdf>=1.24.0    # PDF→画像変換（図面読み込み）
# pdf2image         # PDF→画像変換（poppler依存のため不使用）
# opencv-python     # 画像前処
# 図面クリック計測（クリックで座標取得→実寸変換）
streamlit-image-coordinates>=0.4.0
